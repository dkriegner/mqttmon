Code Comments, function documentation...

TUI-code cleanup

buffer somewhere the lines which are currently on the screen to enable
scrolling also without that this list is updated. This will also allow for
better resize support

proper pause updating including the possibility to scroll the existing messages!

command line parameters for setting the MQTT server settings
or settings windows to change these settings

possibility to subscribe to topics via command line and/or TUI

maximum number of messages saved in buffer to avoid infinite memory growth

more different list modes:
 * only one topic
 * message details

lots of error handling

PEP8 formating of the code!
